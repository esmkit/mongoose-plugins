{
  "private": true,
  "name": "@esmkit/mongoose-plugins",
  "description": "A set of plugins for mongoose.",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Billgo <hi@billgo.me>",
  "bugs": "https://github.com/esmkit/mongoose-plugins/issues",
  "scripts": {
    "start": "bunx kill-port 4000 && bun --hot src/index.ts",
    "publish": "bunx lerna publish --no-git-tag-version --no-push",
    "build": "bunx lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "@devjskit/copy-files": "^1.0.0",
    "@devjskit/kill-port": "^1.0.0",
    "@devjskit/rimraf": "^1.0.0",
    "@types/bun": "^1.0.5",
    "@types/node": "^20.11.7",
    "@types/web": "^0.0.135",
    "lerna": "^8.1.2",
    "mongoose": "^8.1.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}